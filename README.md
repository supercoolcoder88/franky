# franky

Required to install the following tools for Golang pipeline:
- golanglintcli: `curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin`
- govulncheck: `go install golang.org/x/vuln/cmd/govulncheck@latest`
